# Simple Collection of Grafana Scripts

I assume that you have exported specific variables. For example:

If `$HOST` represents the FULLURL for example, you would replace or set to something like `http://localhost:3000`.